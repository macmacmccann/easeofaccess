<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="main_interface.OverlayScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:main_interface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Transparent" CornerRadius="1" Padding="12">
   

        <Border
             x:Name="RootPanel"
                Opacity="0"
    Background="#AA1E1E1E"
    CornerRadius="12"
    Padding="12">

            <StackPanel Spacing="8">

                <TextBlock
            Text="Saved Commands"
            FontSize="16"
            FontWeight="SemiBold"
            Foreground="White"/>

                <ListView
            x:Name="CommandList"
            Background="Transparent"
            BorderThickness="0">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border
                        Background="#33FFFFFF"
                        CornerRadius="6"
                        Padding="8"
                        Margin="0,2">

                                <TextBlock
                            Text="{Binding}"
                            Foreground="White"
                            FontFamily="Consolas"
                               PointerPressed="Command_Clicked"     
                                    />
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>

            </StackPanel>
        </Border>




    </Grid>
</Window>
