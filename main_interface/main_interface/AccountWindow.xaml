<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="main_interface.AccountWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:main_interface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid x:Name="maingrid">

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Padding="24,20,24,28" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" CornerRadius="12" Padding="20,16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="System Status" FontSize="22" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Every toggle currently active on your machine"
                                   FontSize="12" Opacity="0.55" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- Status cards built in code-behind -->
                <StackPanel Grid.Row="1" x:Name="StatusContainer" Spacing="12"/>

            </Grid>
        </ScrollViewer>

    </Grid>
</Window>
