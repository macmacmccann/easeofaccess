<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="main_interface.AccountWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:main_interface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid x:Name="maingrid">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Padding="28,24,28,32" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" CornerRadius="12" Padding="28,22"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="6" VerticalAlignment="Center">
                            <TextBlock Text="System Status"
                                       FontSize="24" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="All feature toggles currently on your machine"
                                       FontSize="13" Opacity="0.55" Foreground="White"/>
                        </StackPanel>

                        <!-- Active count summary -->
                        <Border Grid.Column="1" CornerRadius="10" Padding="20,12,20,14"
                                Margin="28,0,0,0" VerticalAlignment="Center"
                                Background="{ThemeResource ControlFillColorDefaultBrush}">
                            <StackPanel Spacing="2" HorizontalAlignment="Center">
                                <TextBlock x:Name="ActiveCountText"
                                           FontSize="40" FontWeight="Bold"
                                           Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="ActiveLabelText"
                                           FontSize="11" Opacity="0.6"
                                           Foreground="White" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- Two-column card layout -->
                <Grid Grid.Row="1" ColumnSpacing="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel x:Name="LeftColumn"  Spacing="14"/>
                    <StackPanel x:Name="RightColumn" Grid.Column="1" Spacing="14"/>
                </Grid>

            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
